apiVersion: apps/v1
kind: Deployment
metadata:
  name: neuroclima-ngrok-server
  namespace: uoulu
  labels:
    app: neuroclima
    component: ngrok-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: neuroclima
      component: ngrok-server
  template:
    metadata:
      labels:
        app: neuroclima
        component: ngrok-server
    spec:
      containers:
      - name: ngrok
        image: ngrok/ngrok:latest
        command:
        - ngrok
        - http
        - --authtoken=$(NGROK_AUTHTOKEN)
        - --log=stdout
        - neuroclima-server:8000
        env:
        - name: NGROK_AUTHTOKEN
          valueFrom:
            secretKeyRef:
              name: ngrok-secret
              key: authtoken
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
