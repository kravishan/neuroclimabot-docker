# ============================================================================
# Server PVCs - DEPRECATED
# ============================================================================
# Both Redis and Server data have been removed:
# - Redis: Now uses emptyDir (temporary storage)
# - Server data: All data moved to centralized MongoDB
#
# See k8s/server/deployment.yaml for current configuration
# ============================================================================
